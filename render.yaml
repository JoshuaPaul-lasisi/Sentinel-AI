render_config = """
services:
  - type: web
    name: sentinel-ai-ml-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn real_time_fraud_detection:app --host=0.0.0.0 --port=$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
"""

with open('render.yaml', 'w') as f:
    f.write(render_config)

print("render.yaml created - ready for deployment")